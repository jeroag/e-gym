{
  "name": "egym",
  "private": true,
  "version": "1.0.0",
  "description": "E-GYM · runner raíz para arrancar backend y frontend a la vez",
  "scripts": {
    "dev": "concurrently -n back,front -c cyan,yellow -k --kill-others-on-fail \"pnpm dev:back\" \"pnpm dev:front\"",
    "dev:back": "cd backend && pnpm dev",
    "dev:front": "cd frontend && pnpm dev",
    "install:all": "pnpm install && cd backend && pnpm install && cd ../frontend && pnpm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
